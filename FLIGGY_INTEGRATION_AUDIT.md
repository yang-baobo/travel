# 飞猪 FlyAI 接入前审计

> 审计日期：2026-08-21  
> 审计范围：当前旅行 App 项目的本地代码与配置  
> 本阶段目标：仅梳理真实数据链路、静态/模拟数据边界以及 FlyAI 的推荐接入位置，不安装 FlyAI、不修改业务代码。

## 1. 审计结论

当前项目具备接入飞猪 FlyAI 的基本条件，但在真正接入之前必须先统一酒店和行程状态的数据边界。

最重要的发现如下：

1. 项目现在并行存在两套旅行数据链路：
   - **北京真实地点链路**：首页 → 实时地点列表 → 地点详情 → 实时行程，景点、酒店和餐饮来自高德 Web 服务，路线也来自高德。
   - **深圳静态演示链路**：偏好设置/自定义路线 → 路线规划、酒店、航班和费用计算，核心数据来自本地静态文件或模拟生成器。
2. 当前“酒店列表”和“自定义路线中的酒店选择”不是同一个状态源：酒店列表直接读取静态数组，自定义路线又在页面内部维护自己的 `selectedHotelIds`。现有 `useHotelStore` 并未成为统一数据源。
3. 当前自定义路线生成会把静态酒店、模拟航班和部分估算交通时间真正带入路线编排和费用计算，因此它们不只是展示用 Mock。
4. 项目已经有可复用的安全后端基础：Expo/React Native 前端通过 `/api/...` 调用 FastAPI，服务端环境变量不会打进客户端。FlyAI 凭证应继续只保存在服务端。
5. FlyAI 最适合先替换**酒店搜索与酒店选择的数据源**，再逐步替换航班；高德应保留为地图、坐标补全、POI 地理信息和交通路线服务，不应被 FlyAI 整体替换。
6. 火车票目前没有现成的页面、类型、Store 或路线链路，后续接入属于新增能力，而不是替换已有模块。
7. 仓库中没有发现 FlyAI/Fliggy 代码、依赖或 `FLYAI_API_KEY` 配置。第二阶段必须按官方 CLI 流程验证，不能臆造私有 REST 地址。

## 2. 当前技术架构

### 2.1 客户端

- Expo 54 + React Native 0.81.5 + TypeScript。
- 应用入口为 `index.ts` → `App.tsx` → `src/navigation/AppNavigator.tsx`。
- 状态管理使用 Zustand，部分 Store 使用持久化，酒店实时地点 Store 和旧酒店 Store 当前未持久化。
- 网络请求统一基础封装位于 `src/services/apiClient.ts`：
  - Web 默认使用同源 `/api/...`；
  - 原生端通过 `EXPO_PUBLIC_API_BASE_URL` 指向后端；
  - 已有超时、取消和错误映射。

### 2.2 服务端

- FastAPI 入口：`api/index.py`。
- 当前真实旅行数据适配：`api/travel_providers.py`。
- 已有高德 POI、路线规划、AI、盲盒和 OR-Tools 路线优化等服务端接口。
- `vercel.json` 已配置 Expo Web 构建、Python Serverless Functions 及 `/api/...` 重写。
- 当前代码层面具备“前端只访问自有后端、第三方密钥只在后端读取”的安全结构。

### 2.3 部署状态

- 仓库存在 Vercel 部署配置，因此可以判断项目**为 Vercel 部署做过配置**。
- 当前本地仓库没有足够证据证明线上部署、域名或最新部署版本正在生效；这需要在后续发布阶段通过 Vercel 项目或线上地址单独确认。
- 当前 `.git` 中未发现可用于本次审计确认协作远端和部署分支的有效信息，因此本报告不把“本地可运行”等同于“线上已发布”。

## 3. 当前有效页面与两条数据链路

### 3.1 北京真实地点链路

主要链路：

`HomeScreen` → `LivePlacesScreen` → `LivePlaceDetailScreen` → `LiveItineraryScreen`

数据流：

`useLiveTravelStore` → `travelDataService.ts` → `/api/travel/places` 或 `/api/travel/routes` → `api/travel_providers.py` → 高德 Web 服务

特点：

- 当前城市固定为北京。
- 景点、酒店、餐厅都由高德 POI 搜索返回。
- 地点详情可加入实时行程。
- 实时行程基于真实坐标调用高德公交、驾车或步行路线。
- 列表已有加载、错误重试、空结果和分页状态。
- 酒店的 `cost` 只是高德商户字段中的参考消费，不能当作可售房型、实时库存或严格的每晚价格。
- 携程和美团目前只是可选的跳转链接模板，不是酒店/商品库存 API。

### 3.2 深圳静态演示链路

主要链路：

`PreferenceScreen` / `LiveItineraryScreen` → `ExploreMainScreen` 或 `RoutePlanScreen`

特点：

- 导航中已将旧探索页标记为“历史演示数据”，但该入口仍可到达。
- `RoutePlanScreen` 仍直接消费静态深圳酒店、景点、餐厅和模拟航班。
- 路线优化算法可以调用真实 OR-Tools 后端，但传入的酒店、航班及部分交通时间事实仍可能是静态或估算数据。
- 这条链路是 FlyAI 接入时最需要清理的数据源边界。

### 3.3 重复文件风险

仓库存在若干带 `(1)` 后缀的重复文件，例如 `App(1).tsx`、`index(1).ts`、部分配置副本。当前入口使用不带 `(1)` 的文件。后续接入不得批量同步修改这些副本，除非先证明它们在实际构建链路中被引用。

## 4. 酒店模块审计

### 4.1 当前酒店数据源

| 使用场景 | 当前数据源 | 是否真实 | 是否参与正式逻辑 |
| --- | --- | --- | --- |
| 北京实时地点中的酒店 | 高德 POI | 地点真实；价格/房型/库存不完整 | 参与实时地点和实时行程 |
| 旧酒店列表 | `src/data/hotels.ts` + `src/data/additionalHotels.ts` | 静态演示数据 | 参与列表筛选与展示 |
| 自定义路线中的酒店 | 直接导入上述静态酒店数组 | 静态演示数据 | **参与推荐、路线锚点、早餐和费用计算** |
| 酒店房型 | 本地默认房型和价格倍数 | 模拟生成 | 参与详情和费用计算 |
| 订单酒店详情 | 根据 `hotelId` 回查静态酒店 | 静态演示数据 | 参与订单详情展示 |

### 4.2 搜索、筛选和卡片

`src/screens/explore/HotelListScreen.tsx` 当前直接导入静态 `hotels`，在客户端执行：

- 深圳固定区域筛选；
- 经济型/中端/豪华等本地等级筛选；
- 本地价格区间筛选；
- 设施筛选；
- 酒店名和描述搜索；
- 评分/价格排序。

卡片会强制展示数值型“每晚价格”、评分、等级、区域、设施和静态图片。它没有处理第三方字段缺失、价格标签、无价状态、真实预订链接或数据来源标识。

当前列表卡片没有完成可用的酒店选择/详情跳转闭环。`HotelDetailScreen` 虽然存在，但注册在订单导航栈中，现有酒店列表并未导航到它。

### 4.3 酒店选择最终写入哪里

- `useHotelStore.ts` 有 `selectedHotelId`，但当前酒店列表和自定义路线都没有把它当成权威来源。
- `RoutePlanScreen.tsx` 使用页面内局部状态 `selectedHotelIds: Record<number, string>`，按晚保存酒店。
- 结算时只把 `selectedHotelIds[1]` 作为单个 `hotelId` 传入，后续夜晚或换酒店信息会丢失。
- `useRouteStore.ts` 持久化路线景点和日程，但不保存选中酒店、航班或实时地点。
- `Order` 也只支持一个可选 `hotelId`，订单详情再用该 ID 回查静态数组。

结论：当前不存在完整、统一、可持久化的 Trip 酒店单一事实源。FlyAI 接入前必须选定现有路线/旅行状态作为权威来源并扩展它，不能再创建一套互不相通的选择状态。

### 4.4 现有酒店类型的兼容问题

旧 `Hotel` 类型强制要求：

- 数值型 `pricePerNight` 和 `rating`；
- 本地区域、等级、描述、图片、设施和坐标；
- 静态景点关联。

真实 FlyAI 结果可能缺少其中部分字段，也可能只提供价格文案、不同币种、产品链接或平台原始 ID。后续应先归一化为应用自己的酒店领域模型，并允许未知值；UI 不得为了满足旧类型而编造价格、评分、设施、图片或坐标。

## 5. 航班、火车票、景点与旅行商品审计

### 5.1 航班

`src/data/flights.ts` 是模拟航班生成器：

- 航线固定为大连（DLC）与深圳（SZX）往返；
- 航空公司、日期价格矩阵和航班号由本地配置生成；
- 起降时间、价格及波动包含本地伪随机/种子逻辑；
- 没有真实库存、票价有效期、供应商来源或预订链接。

`FlightSearchScreen.tsx` 与 `RoutePlanScreen.tsx` 都直接使用这套 Mock。自定义路线会自动选择往返模拟航班，并将其时间和价格带入日程、校验和总费用，因此这不是单纯的视觉占位数据。

### 5.2 火车票

当前未发现火车/高铁相关的领域类型、搜索页面、Store、服务层、路线选择或订单数据流。FlyAI 火车票能力需要作为新模块设计，不能照搬现有航班结构后假设字段一致。

### 5.3 景点和 POI

项目有两类景点数据：

- 北京实时入口通过高德搜索真实景点 POI；
- 历史探索和自定义路线继续使用 `src/data/attractions.ts`、`src/data/additionalAttractions.ts` 等静态深圳数据。

后续 FlyAI 更适合补充“可售景点门票/旅行商品、价格和跳转链接”，高德继续负责地理 POI、坐标、附近搜索与路线。两者需要用名称、地址、坐标或稳定映射做匹配，不能仅靠数组下标或展示名强行合并。

### 5.4 餐饮与其他商品

- 北京餐饮地点已由高德真实 POI 提供。
- 自定义路线中的餐厅仍来自本地静态数据并参与排餐。
- 当前携程/美团只提供跳转模板；没有真实商品查询、库存或统一支付能力。
- 本次只审计飞猪，后续 FlyAI 接入不应顺带修改高德、语音、GLM、盲盒或支付模块。

## 6. 路线生成如何使用这些数据

### 6.1 实时行程

`LiveItineraryScreen` 使用用户加入的 `TravelPlace` 坐标调用 `/api/travel/routes`，由高德计算公共交通、驾车和步行路线。这条链路的地理路线是真实数据链路。

限制：实时地点和实时行程只存在于未持久化的 `useLiveTravelStore` 中，尚未和自定义路线、购物车或订单统一。

### 6.2 自定义路线

`RoutePlanScreen` 使用：

- 静态景点；
- 静态酒店；
- 静态餐厅；
- 模拟航班；
- 本地设施、早餐和房型推导；
- `universalRoute.ts` 的时间矩阵或坐标估算。

随后把交通时间矩阵发送到 `/api/travel/optimize-route`，由 OR-Tools 执行优化。也就是说，优化器本身可以是真实后端算法，但“喂给优化器的旅行事实”并不全是真实数据。

`universalRoute.ts` 的同步路径优先读取硬编码时间矩阵，缺失时使用坐标估算，部分结果还会加入随机扰动。异步高德路径已经存在，但当前自定义路线的主要计算仍大量使用同步估算路径。

### 6.3 AI 路线生成

`src/utils/routeGenerator.ts` 同样直接导入静态景点、餐厅和酒店，自动选酒店并计算费用，然后只把景点/日程写入路线 Store。酒店选择没有被作为完整 Trip 状态持久化。

## 7. Mock、硬编码和真实数据边界

| 数据/逻辑 | 类型 | 是否仍在可达运行链路 | 后续处理建议 |
| --- | --- | --- | --- |
| 北京高德景点/酒店/餐厅 | 真实 POI | 是 | 保留；酒店字段不能冒充实时库存 |
| 高德交通路线 | 真实路线服务 | 是 | 保留 |
| 深圳静态酒店 | 硬编码 | 是 | 酒店阶段由归一化 FlyAI 服务替换生产入口 |
| 本地默认房型/价格倍数 | 模拟 | 是 | 不得作为真实房价继续展示 |
| 大连—深圳航班 | 模拟生成 | 是 | 航班阶段由 FlyAI 替换 |
| 深圳静态景点/餐厅 | 硬编码 | 是 | 暂时保留演示；后续逐模块替换 |
| 同步交通时间矩阵/坐标估算 | 硬编码/估算 | 是 | 后续逐步切到高德真实路线并保留降级 |
| OR-Tools 路线优化 | 真实算法 | 是 | 保留，改为消费归一化真实数据 |
| 携程/美团模板链接 | 外部跳转模板 | 可选 | 不是实时数据源，不与 FlyAI 混为一谈 |
| 盲盒高德候选 | 真实 POI + 业务筛选 | 是 | 本次不修改 |

## 8. 推荐的 FlyAI 接入层

推荐保持以下单向依赖：

`页面/UI` → `TravelHotelService` → `自有后端 FlyAI Adapter` → `飞猪官方 FlyAI CLI/能力`

具体原则：

1. **客户端不直接调用 FlyAI**，也不保存 `FLYAI_API_KEY`。
2. 后端 Adapter 是唯一理解 FlyAI 原始字段的层；页面只消费应用自己的归一化模型。
3. `TravelHotelService` 负责搜索参数、请求状态、分页/重试和应用模型，不在页面内拼第三方参数。
4. 高德继续负责 POI 地理信息和路线；FlyAI 负责酒店、航班、火车票、景点/旅行商品等旅行供给信息。
5. 真实字段缺失时返回 `null`、空集合或明确状态，不伪造价格、评分、图片、设施或预订 URL。
6. 官方 CLI 的命令、返回结构、可用技能和鉴权方式必须在第二阶段安装后实测并记录；本阶段不假设不存在于官方文档中的 REST 接口。

### 8.1 为什么先接酒店

- 酒店同时影响列表、详情、自定义路线锚点、早餐、费用和订单，是目前耦合最深的数据源。
- 高德已经能提供北京酒店地点，但缺少可售商品级信息；FlyAI 可以在不替换地图能力的情况下补足搜索结果和预订跳转。
- 先打通酒店能验证 CLI、后端密钥、字段归一化、错误降级和 UI 空值处理，再复用到航班与其他商品。

### 8.2 建议的归一化酒店模型

后续模型至少应支持：

- `id`：应用内稳定 ID；
- `source`、`rawSourceId`；
- `name`、`address`、`city`、可选坐标；
- 可选 `rating`、`starLevel`、`description`、`amenities`、`images`；
- 可选数值价格、币种和原始价格文案；
- `bookingUrl`；
- 数据更新时间/不确定性；
- 原始结果仅保留在 Adapter 或服务端调试结构中，不直接渗透到 UI。

## 9. 后续阶段预计涉及的文件

以下只是变更清单，**本阶段没有修改这些文件**。

### 9.1 服务端与配置

- `api/index.py`：注册 FlyAI 酒店搜索等自有后端接口。
- `api/fliggy_adapter.py` 或独立 `api/fliggy/`：封装官方 CLI 调用、超时、错误映射和字段归一化。最终文件形态取决于第二阶段对官方 CLI 运行时的验证。
- `package.json` / `package-lock.json`：如果官方 CLI/SDK 以 Node 包运行，则加入官方依赖和安全脚本。
- `requirements.txt` / `pyproject.toml`：只有选择 Python 后端桥接方案时才调整。
- `vercel.json`：仅在新增 Node/Python Function 路由或运行时配置确有需要时修改。
- `.env.example`：新增仅服务端使用的 `FLYAI_API_KEY` 说明，禁止使用 `EXPO_PUBLIC_` 前缀。
- `tests/`：新增 Adapter、归一化、错误降级和后端接口测试。

### 9.2 客户端服务与类型

- 新建 `src/services/travelData/hotel/TravelHotelService.ts`：页面唯一酒店搜索入口。
- 新建 `src/services/travelData/fliggy/FliggyAdapter.ts` 或同职责模块：只调用自有后端并映射 DTO；不得持有密钥。
- 新建归一化酒店类型，或谨慎扩展 `src/types/index.ts` / `src/types/travel.ts`。
- `src/services/apiClient.ts`：仅在需要统一新增错误类型、超时或请求能力时扩展。
- `src/store/useHotelStore.ts`：从静态数组改为服务结果，并与路线选择的权威状态对齐。
- `src/store/useRouteStore.ts`：建议扩展为选中酒店/航班的现有旅行状态载体，避免另建第二套 Trip 状态；最终归属在酒店 UI 接入前确定。

### 9.3 页面和路线消费层

- `src/screens/explore/HotelListScreen.tsx`：接真实搜索、加载、空状态、错误和缺失字段展示。
- `src/screens/explore/HotelDetailScreen.tsx`：改为消费归一化酒店快照/服务结果，而不是静态 ID 回查。
- `src/screens/custom/RoutePlanScreen.tsx`：从统一状态读取真实酒店；在酒店链路稳定后再替换。
- `src/utils/routeGenerator.ts`、`src/utils/universalRoute.ts`、`src/utils/mealScheduler.ts`、`src/utils/costCalculator.ts`：后续改为消费归一化数据，去除对静态酒店事实的依赖。
- `src/screens/orders/OrderDetailScreen.tsx` 与订单类型/Store：保存必要的酒店快照和外部跳转信息，避免订单依赖易变的搜索结果。

### 9.4 航班、火车和旅行商品的后续文件

- 航班阶段：`src/data/flights.ts`、`FlightSearchScreen.tsx`、`RoutePlanScreen.tsx` 及 `Flight` 类型。
- 火车阶段：新增独立类型、服务、页面和选择状态；当前没有可直接替换的模块。
- 景点/旅行商品阶段：扩展 `TravelPlace` 的来源/商品关联，保留高德 POI 和路线能力。

## 10. 明确不应在 FlyAI 接入中顺带修改的范围

- 高德 API 的现有地点与路线实现；
- StepAudio ASR、Realtime 语音；
- GLM 和中转站配置；
- 盲盒 Skill 逻辑；
- 支付、合单或分账；
- 现有视觉设计的大规模重构；
- 带 `(1)` 后缀的重复文件；
- 携程/美团链接策略，除非后续单独提出迁移需求。

## 11. 第二阶段开始前的验证门槛

第二阶段只做官方 CLI/技能验证，开始前应满足：

1. 明确官方 `@fly-ai/flyai-cli` 的安装方式和所需 Node 版本。
2. 在本地服务端环境中配置 `FLYAI_API_KEY`，不输出、不提交、不传入前端。
3. 实际执行官方 CLI 的帮助/技能列表命令，记录真实命令和返回，不猜测技能名。
4. 验证北京酒店搜索至少一条真实结果，并保存一份脱敏后的真实字段样例用于归一化设计。
5. 验证 CLI 是否适合 Vercel Serverless 运行；若不适合，再决定是否使用独立 Node 服务。不能在验证前改变现有 FastAPI 架构。
6. 分清“酒店地点”与“可售酒店商品”：高德结果可以作为坐标补全，FlyAI 结果作为旅行商品来源，任何合并都必须可追溯。

## 12. 本阶段完成状态

- 已完成项目结构、数据源、路线消费、后端和部署配置审计。
- 已定位所有会影响 FlyAI 酒店首轮接入的主要文件与状态断点。
- 未安装 FlyAI CLI。
- 未调用飞猪能力。
- 未修改任何现有代码、配置、密钥、高德、语音、GLM 或业务页面。
- 下一步应严格进入“官方 CLI 与技能验证”，验证通过后才开始酒店接口实现。
